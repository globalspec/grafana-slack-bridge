---
# Dedicated ServiceAccount for the bridge. Vault's Kubernetes auth backend
# binds to this SA when the SecretProviderClass mounts secrets at pod start.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: grafana-slack-bridge
  namespace: grafana
  labels:
    app.kubernetes.io/name: grafana-slack-bridge
